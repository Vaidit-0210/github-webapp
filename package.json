{
  "name": "github---mern",
  "version": "1.0.0",
  "description": "A MERN stack application with GitHub OAuth integration",
  "keywords": [
    "mern",
    "react",
    "express",
    "mongodb",
    "github-oauth"
  ],
  "author": "Vaidit Patel",
  "license": "ISC",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend"
  }
}
